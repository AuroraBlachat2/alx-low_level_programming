#include <stdio.h>
/**
 * main - print_alphabet check the code
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
	char d;

	for (d = 'a'; d <= 'z'; d++)
	{
		_putchar(d);
	}

	_putchar('\n');

	return (0);
}
